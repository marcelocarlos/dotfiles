# Created by Marcelo C. Carlos
# Some aliases where based on those found on: https://github.com/mathiasbynens/dotfiles

# use neovim
alias vi="vim"
alias vim="nvim"

# Navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias -- -="cd -"
alias la="ls -Gla"
alias lsd='ls -l | grep "^d"'
alias ll='ls -l'
alias lh='ls -lh'

# Always use color output for `ls`
alias ls="command ls --color"

# Make RM a little safer
alias rm='rm -I --preserve-root'

# Tree
alias tree='tree -C'

# Colorize grep command output
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# Always resume wget
alias wget='wget -c'

# colorize diff
alias diff='colordiff'

# other
alias timestamp='date +%s'
alias datebkp='date +"%Y%m%d-%H%M%S"'
alias reset_title="export PROMPT_COMMAND='echo -ne \"\033]0;${PWD/#$HOME/~}\007\"'"

# Start calculator with math support
alias bc='bc -l'

# cheatsheets
alias cs='cheatsheets.sh'
alias csl='cheatsheets.sh -l'

# snippets
alias sv='snippets.sh'
alias sl='snippets.sh -l'

# marks
alias m='mark'
alias j='jump'

# Enable aliases to be sudoed
alias sudo='sudo '

# Get OS X Software Updates, update Homebrew itself, and upgrade installed Homebrew packages
alias update-all-software='sudo softwareupdate -i -a; brew update; brew upgrade'

# IP, Whois, etc
alias public_ip="curl ifconfig.me"

# show mounts in a formatter way
alias mountf="mount | column -t"

# View HTTP traffic
alias sniff="sudo ngrep -d 'en1' -t '^(GET|POST) ' 'tcp and port 80'"
alias httpdump="sudo tcpdump -i en1 -n -s 0 -w - | grep -a -o -E \"Host\: .*|GET \/.*\""
alias sniffpw="sudo tcpdump -i en1 port http or port ftp or port smtp or port imap or port pop3 -l -A | egrep -i 'pass=|pwd=|log=|login=|user=|username=|pw=|passw=|passwd=|password=|pass:|user:|username:|password:|login:|pass |user ' --color=auto --line-buffered -B20"

# Proverif aliases
alias pv="proverif -color"
alias pvtex="proveriftotex"
alias pvcss="proverifaddcss"
alias pvhtml="proverifhtml"

# Canonical hex dump; some systems have this symlinked
type -t hd > /dev/null || alias hd="hexdump -C"

# OS X has no `md5sum`, so use `md5` as a fallback
type -t md5sum > /dev/null || alias md5sum="md5"

# Recursively delete `.DS_Store` files
alias cleanup-folder="find . -type f -name '*.DS_Store' -ls -delete"

# Check TRIM status
alias trim-status="system_profiler SPSerialATADataType | grep -i TRIM"

# TODO
alias td="vi ~/Google\ Drive/todo.md"

# Use `open` to open files with in linux
if [[ "$OSTYPE" =~ ^linux ]]; then
    alias open="xdg-open"
fi

# git stuff
alias gcp="git commit --author='Marcelo Carlos <contact@marcelocarlos.com>'"

# fix crontab on OSX
alias crontab="VIM_CRONTAB=true crontab"

# documentation output by default in rspec
alias rspec='rspec --format=documentation'

alias be='bundle exec'
