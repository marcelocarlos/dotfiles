# Create a new directory and enter it
function md() {
	mkdir -p "$@" && cd "$@"
}
export -f md

function realpath() {
	local FOLDER="$1"
	if [ -f $1 ]; then 
		FOLDER=`dirname "$1"`
	fi
	cd $FOLDER
	echo $PWD
	cd - >> /dev/null
	return 0
}
export -f realpath

